import { InputFilePhotosDirective } from './input-file-photos.directive';

describe('InputFilePhotosDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFilePhotosDirective();
    expect(directive).toBeTruthy();
  });
});
